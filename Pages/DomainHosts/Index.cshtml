@page
@model Domain_Name_Ownership_Core_Web.Pages.DomainHosts.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Domain Hosts</h1>

<p>
    <a class="btn btn-primary  rounded-pill" asp-page="Create">Add Domain Host</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DomainHost[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DomainHost[0].DomainTypes)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.DomainHost) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DomainTypes)
            </td>
            <td>
                <a class="btn btn-primary  rounded-pill" asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a class="btn btn-primary  rounded-pill" asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a class="btn btn-primary rounded-pill" asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
